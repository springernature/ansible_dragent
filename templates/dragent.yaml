customerid: {{ dragent_api_key }}
{% if dragent_tags is defined %}
tags: {% for item in item.value.dragent_tags -%}
{{ item.name }}:{{ item.value }}
{%- if not loop.last -%},{%- endif -%}
{%- endfor -%}
{%- endif -%}